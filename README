this application creates an icon in a KDE4+ systemtask bar
the icon is changeable via a qdbus command

qdbus $(qdbus | grep kstatusicon) /TopLevel  Set \
"com.vandeenensupport.TopLevel" "iconfile" "/usr/share/zenity/zenity-calendar.png"

changes the visible icon of the kstatusicon instance. 
